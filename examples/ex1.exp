include 'lib/std'

new W H L.

W << 20
H << 10

L << W; L <* H

new x y pos.
new r.

def (draw) beg
	x << y << pos
	x <% W
	y </ W
	
	pos inc
	
	pos print newline
	
	if pos < L then DO (draw) end DO
end def

(draw)
